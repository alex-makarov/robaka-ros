segmentation_layer:
  # Topic of semantic segmentation output
  segmentation_data: /camera/color/semantic
  # Folder with parameters.yaml
  homography_folder: /home/alex/src/catkin_ws/src/robaka-ros/conf/semantic-segmentation
  # Zero-indexed IDs marked as an osbtable
  obstacle_ids_str: 1
  # Zero-indexed ID to clear obstacles
  path_ids_str: 44
  # meters to the left/right of the bot to modify costmap
  x_range: 5.0
  # meters in front of the bot to modify costmap
  y_range: 5.0
